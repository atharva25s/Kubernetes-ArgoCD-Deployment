apiVersion: v1
kind: Service
metadata:
  name: backend-car-app-service
  namespace: default
spec:
  selector:
    app: backend-car-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000   
  type: NodePort
