I have set up aws-cli locally to help terraform access my account to provision infrastructure. 
You need to write a separate provider.tf file that includes authentication IAM code. 
The ssh .pem file is stored locally on my device.
That specific file needs to be present on your device as well. 
